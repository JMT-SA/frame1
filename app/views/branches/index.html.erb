<p id="notice"><%= notice %></p>

<h1>Branches</h1>

<table>
  <thead>
    <tr>
      <th>Branch name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @branches.each do |branch| %>
      <tr>
        <td><%= branch.branch_name %></td>
        <td><%= link_to 'Show', branch %></td>
        <td><%= link_to 'Edit', edit_branch_path(branch) %></td>
        <td><%= link_to 'Destroy', branch, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Branch', new_branch_path %>
  <h2>Block 1</h2>
<div style="height:20em">
  <div id="myGrid" style="height: 100%;" class="ag-fresh" data-gridurl="branches/load_index.json" data-grid="grid"></div>
</div>
  <h2>Block 2</h2>
<div style="height:20em">
  <div id="myGrid2" style="height: 100%;" class="ag-fresh" data-gridurl="departments/load_index.json" data-grid="grid"></div>
</div>
